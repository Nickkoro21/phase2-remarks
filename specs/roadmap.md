# Roadmap — FDMS

> **Κατάσταση: 2026-09-10** (γύρος P46-A4). Ενημερώθηκε με απόφανση του Διοικητή
> Σμήνους της ίδιας μέρας: *«Ενημέρωσε roadmap.»*
>
> **Κανόνας αυτού του αρχείου:** κάθε `[x]` δείχνει τον γύρο ή το commit που το
> έκανε, και κάθε `[ ]` είναι ανοιχτό **σήμερα**. Ό,τι δεν μπορεί να δείξει
> τεκμήριο δεν γράφεται ως έτοιμο.
>
> Ο κανόνας **ελέγχθηκε σε όλο το αρχείο** την ίδια μέρα που γράφτηκε: και τα
> **25** ολοκληρωμένα δείχνουν commit, γύρο ή το αρχείο-τεκμήριό τους. Και **κάθε
> αριθμός εδώ γράφεται αφού τρέξει αυτό που τον παράγει**, ποτέ από μνήμη:
> ο αριθμός των fixtures της φάσης Δ γράφτηκε μία φορά εφευρημένος και το έπιασε η
> επαλήθευση του ίδιου του γύρου (`bridge-spec.md` § 19ε·2, V1).

## Phase A — Data layer ✅ (Ιούλιος 2026)

- [x] Ψηφιοποίηση Performance Criteria (118 items) + πίνακες MIF (9) με LLM-as-judge
      (`13384b3` — `data/criteria` · `data/mif`)
- [x] Resolution παραπομπών «As described» σε πραγματικές τιμές + αποφάσεις χρήστη
      (`13384b3` — `data/USER_DECISIONS.md`· ορατές στο item Info από `56d7013`)
- [x] Απαιτήσεις Phase II (379) + board_tolerance_matrix (`13384b3`· καρτέλα και
      board matrix `87eab00` · 114 items → 430 links `c146758`)
- [x] Human Factors phrase bank (91 έννοιες, applicability-tagged) — `13384b3`
      (`data/human_factors.json`)
- [x] Διόρθωση MIF: το κενό κελί T/O της C4901-05 ήταν **τυπογραφικό του συλλαβικού** και η απόφανση το γέμισε με κώδικα 3 (`0e5f7ae`)

## Phase B — Phase 2 Remarks app ✅ (ολοκληρώθηκε 2026-09-04)

- [x] Static SPA: κατηγορία → item → κώδικες → παρατηρήσεις → copy (`13384b3`,
      μόνο-αγγλικό UI + GitHub Pages `2f23da8`)
- [x] Master index + lazy loading (`13384b3` · v2 trunk με fallback `1993671`)
- [x] **Παρατηρήσεις σε ΟΛΟΝ τον στόλο** — Contact · Instrument · Formation · VFR Navigation.
      Η παλιά γραμμή «συμπλήρωση observations για Instrument / Formation / VFR Navigation»
      **έκλεισε**: Γύρος 26 (`2d9b92a`) · Γύρος Β (`19e7280`) · Γύρος Γ (`7e5c43a`) ·
      Γύρος Δ (`984ec04` + `9ae8838`). **117 αρχεία, 1 882 modes, 5 646 κείμενα `texts_at`** —
      `specs/observations-style.md` § 15 · § 16
- [x] Ορατά ids ανά mode (απόφανση 4, Γύρος 26) — `observations-style.md` § 10
- [x] **Ο αριθμός στο ίσο έγινε το όριο** (απόφανση ιδιοκτήτη 2026-09-04): ελεγκτής με
      κάδους `AT-NUMBER` / `AT-QUALITATIVE`, μηχανικό πέρασμα 1 108 κειμένων
      (`specs/round-d-at-fix-log.csv`), 18 πακέτα γραφέων, 1 833 κείμενα από αναιρέτες.
      **Πύλες κλεισίματος: `ERROR 0 · AT-NUMBER 0 · AT-QUALITATIVE 0 · FALLBACK 0`**
- [x] Description — ο δίδυμος των Remarks: ολόκληρο μπλοκ ΦΒ από μία έξοδο (`cb62233`,
      εμπλουτισμός `22d15b9`)
- [ ] **Ανθρώπινη επικύρωση δείγματος παρατηρήσεων ανά κατηγορία** — δεν έχει γίνει σε
      καμία κατηγορία· είναι δουλειά ανθρώπου, όχι γύρου
- [ ] **Τα 641 pooled ποιοτικά `texts_at` που έμειναν χωρίς αριθμό** — καθένα με
      **ονομαστικό λόγο ανά κείμενο** (checklist, κλήσεις, clearing, όρια Flight Manual —
      `observations-style.md` § 5.7 · § 16). Ανοιχτό **ως απόφαση**, όχι ως χρέος: αν ο
      Διοικητής Σμήνους κρίνει ότι κάποια από αυτά *πρέπει* να φέρουν αριθμό, είναι γύρος
      γραφέων· αλλιώς κλείνει ως έχει

## Phase C — Ο Scheduler (FDMS) ✅ ως προς το εύρος του, ενεργός

Προδιαγραφή: `specs/scheduler-spec.md`. **Εκτός σκοπού, δηλωμένα**: βαθμολόγηση σε
επίπεδο ΠΑΠ / Φύλλων Βαθμολογίας — τρέχουν άλλα συστήματα.

- [x] **Training log** ανά κόμβο του συλλαβικού γράφου, με scope μαθητή / τάξης,
      ειδικές εξόδους, ΝFS (Α0473) και **διάρκεια** (ώρες, από 05/09/2026) —
      `4ef1f54` (SchedStore + log + readiness) · `7e106ed` (μαθήματα ανά course) ·
      `d0d7160` (NFS, badges, 153/153 node audit) · `eb2c62d` (διάρκεια)
- [x] **Ημερήσιο πλάνο (board)**: κύματα, F/S, μαθήματα, alternates, print view,
      actualize → events του log — `4ef1f54` (D+1 board, slots, gates, idempotent
      actualize) · `9e20060` (καθήκοντα ανά κύμα, daily-load engine) ·
      `174bdb6` (RSU ανά κύμα, κώδικες απουσίας) · `a32512d` (ταφή του κινδύνου)
- [x] **Progress editor** ανά μαθητή (πλήρες CRUD πάνω στον γράφο) — `4ef1f54` ·
      `9e20060` (ανασχεδίαση) · `7e106ed` (stepper «ως εδώ» ανά βήμα) ·
      `d0d7160` (ομαδοποίηση κατά ροή του flowchart)
- [x] **Consequences** — οι σειρές της ΠΔ 1γ/1δ, τα κατώφλια και οι παραπομπές
      (`app/schedconsq.js`) — `38d5cbf` (η μηχανή, fail-08…09/10/11/12/16/17/19/45) ·
      `d0d7160` (μετρητές 3-01 με drilldown) · `ad623e0` (ονόματα, όχι κώδικες)
- [x] **Currency** εκπαιδευτών (Ε / Σ, εξάμηνα, αναγγελίες), με κάρτα και σειρά
      call-sign (`9570d7d` · `ffd8a40`)
- [x] **Balance**, duty roster, availability, gates — `4ef1f54` (Balance, gates,
      publish/print) · `9e20060` (SOF-A/B, Ground 1/2, lessons A/B) · `4bb9a9a`
      (απερχόμενοι IP εκτός pickers, lost-instructor, όριο 2 ταυτόχρονων solo) ·
      `16c7c70` (night auto από το Currency)
- [ ] **Ένωση με τα requirements μέσα στην εφαρμογή Remarks** — προβολή απαιτήσεων /
      ορίων ανά Training Section δίπλα στο item. Τα δεδομένα υπάρχουν
      (`data/requirements/*`), η προβολή στο item **δεν** έχει γίνει
- [ ] **Gradesheet composer**: πλήρες ΦΒ (παρατηρήσεις + γενική απόδοση) σε ένα βήμα.
      Τα δύο μισά υπάρχουν (Remarks · Description)· η σύνθεσή τους όχι

## Phase D — Η γέφυρα FDMS ⇄ Wings Ahead ✅ και στις δύο κατευθύνσεις

Προδιαγραφή: `specs/bridge-spec.md`. Fixtures: `node tools/fixtures/bridge/run.js`
→ **1 625 passed, 0 failed**.

- [x] **Slice 1 — read-only cross-check** (`fa28201`), σάρωση ευρημάτων (`6f05f7f`)
- [x] **Φάση 3 — η επιβεβαιωμένη συμπλήρωση WA → FDMS** με change log και ↺ Undo
      (`2945e62`), και η τρύπα + το ψέμα της 3β (`1146c1e`)
- [x] **Φάσεις 4+5 — η γραμμή προς τα έξω**: μια πτήση του training log γίνεται γραμμή
      στο record του μαθητή (`17d64be`), + οι τέσσερις διορθώσεις (`3f91599`), η ηλικία
      της ανάγνωσης (`8289fa1`), τα removals που δεν ήταν removals (`dbc6108`) και οι
      προτάσεις που δεν απεδείκνυαν τον εαυτό τους (`a3a04b3`)
- [x] **Φάση 6α — checkrides και solos εφαρμόσιμα** (`97408f8`)
- [x] **Φάση 6β — η γραμμή Currency** WA → FDMS, μονόδρομη (`474cefa`)
- [x] **Φάση 6γ — η διάρκεια γίνεται πεδίο του FDMS** + απόφανση `SOLO_NG_COMPLETES` +
      το ledger φτάνει στη ζωντανή αναφορά (`eb2c62d`)
- [x] **Φάση 6δ — η διάρκεια περνά το σύρμα** (P46-A4, 10/09/2026): ο γύρος σχήματος του
      Wings Ahead έριξε τον έναν κλάδο, η `wa.chk_duration` πήρε τη θέση του, το
      `duration` είναι το **εντέκατο** κλειδί του `PUSH_ROW_KEYS`· οι δύο writers του
      progress editor ονομάζουν το πεδίο· η αναζήτηση του log βρίσκει πτήση από τις ώρες
      της. **Η απόφανση #8 έκλεισε και στα δύο μισά** — `bridge-spec.md` § 19
- [ ] **ΤΟ ΕΒΔΟΜΑΔΙΑΙΟ ΚΡΥΠΤΟΓΡΑΦΗΜΕΝΟ BACKUP** στο ιδιωτικό `fdms-data` — **απόφανση #7,
      αρχή ελάχιστης διαρροής**. Το μόνο κομμάτι της απόφανσης #7 που δεν έχει γίνει
      (`bridge-spec.md` § 11, slice 5)
- [ ] **Τα κοσμητικά υπόλοιπα του P45-FDMSe verify** (κανένα δεν γράφει λάθος στο store):
      το strand hint με τους τρεις διαζευκτικούς όρους όπου το `evChanged` ξέρει ποιος
      ισχύει· το OID-case banner που υπερ-ισχυρίζεται για instructors, δεν τους ονομάζει
      στο Identities και δεν κάνει dedupe· ο κλάδος «no fate» του `movedTail` που κανένα
      probe δεν φτάνει· ένα hold key που τυπώνεται ωμό αντί για `HOLD_WORD`
- [ ] **Ζωντανός περίπατος του P46-A4** — αναζήτηση με «1,3», ώθηση που κουβαλά ώρες, και
      μια χαλασμένη ώρα στο «Not crossing». Πράξη της κύριας συνεδρίας, με ψεύτικο export

### Βήματα που ανήκουν στον χρήστη, όχι σε γύρο

- [ ] **Mint του bridge credential** στο Wings Ahead (People → Bridge) — γίνεται **μόνο**
      από τον admin στην οθόνη του, ποτέ από deploy· έπειτα ⚚ ⚙ paste στο FDMS, Test
      (= pull), Live on. Στο cloud είναι ακόμη **never minted**
- [ ] **Διαγραφή του δοκιμαστικού προσώπου** από το cloud roster
- [ ] **Χειροκίνητες δοκιμές του offline export** από τον ίδιο τον ιδιοκτήτη, στο
      μηχάνημα της μονάδας

## Phase E — Το offline export για τη μονάδα

Προδιαγραφή: `specs/offline-export-spec.md`. Παραδοτέο: **ΕΝΑ** HTML στο
`D:\FDMS-export` + `README-IT` στα ελληνικά.

- [x] Ολόκληρη η εφαρμογή σε ένα αρχείο (`63e39b1`), **δάπεδο Firefox 32** με ES5-parse
      κάθε inline script (`e41623c`), και ο γύρος του verify (`0067303` · `4e1770b`)
- [ ] **ΤΟ ΕΠΟΜΕΝΟ EXPORT ITERATION — απόφανση 28/08/2026 (attribution).** Στο
      **unit-facing** παραδοτέο και **μόνο** εκεί: (α) αφαίρεση του credit block της AI
      από το **παραγόμενο** αρχείο, μέσα στον `tools/build_offline.py` — **ποτέ** με edit
      του footer της εφαρμογής· (β) **αδρανοποίηση των τριών GitHub feedback links** στο
      κλειστό build. Στο δημόσιο repo και στη ζωντανή εφαρμογή το attribution **μένει**,
      κατά ρητή απόφανση

## Αποφάσεις που περιμένουν τον Διοικητή Σμήνους

- [ ] **Rewrite του git history** για τα δύο πραγματικά call signs που έζησαν αυτούσια σε
      `app/currency.js` και `specs/scheduler-spec.md` μέχρι τον Γύρο 18. Αφαιρέθηκαν από
      τα αρχεία (η συμπεριφορά δεν άλλαξε — η σειρά προκύπτει πάντα από το store), αλλά
      **το ιστορικό τα κρατά** από το `acb6b86` και μετά. Το rewrite έχει συνέπειες σε
      κάθε κλώνο: **απόφανση, όχι πράξη γύρου** — `scheduler-spec.md` § 11α
- [ ] **Οι ετικέτες «CO» του Wings Ahead** — ο ρόλος του ιδιοκτήτη στη μονάδα είναι
      Διοικητής Σμήνους, και το WA τις γράφει αλλιώς. Εκκρεμεί απόφανση για το αν και πού
      αλλάζουν
- [ ] **Τα 641 pooled ποιοτικά κείμενα** (βλ. Phase B) — απόφανση για το αν κάποιο από
      αυτά *πρέπει* να φέρει αριθμό

## Εκκρεμότητες της πλευράς Wings Ahead (χωριστό repo, πύλη § 4φ)

Δεν είναι δουλειά αυτού του repo και **δεν** γίνονται από agent — κάθε αλλαγή σχήματος
περνά από την πύλη § 4φ με τον λόγο του χρήστη.

> **ΤΟ ΕΡΓΑΣΤΗΡΙΟ ΤΗΣ ΠΥΛΗΣ ΔΕΝ ΕΙΝΑΙ ΠΙΑ ΤΟ DOCKER** (10/09/2026). Το Docker Desktop
> σπάει επαναλαμβανόμενα σε αυτό το μηχάνημα· η τοπική απόδειξη γίνεται σε **native
> PostgreSQL μέσα στο WSL Ubuntu**, όπου το `db/schema.sql` τρέχει αυτούσιο. Οι αναφορές
> σε `docker exec supabase_db_WingsAhead` μέσα στο `bridge-spec.md` είναι **ιστορικό**
> παλιότερων γύρων και μένουν ως έχουν. Λεπτομέρειες: `wingsahead-spec.md` § 4χ.

- [ ] Το `&&` guard στο `admin.js` (`CL.exams.claimed`)
- [ ] Το `migrateRecord` τοποθετεί ένα slotless solo **κατά θέση** και όχι κατά
      candidacy (προϋπάρχον, χαμηλή βεβαιότητα πρόθεσης)
- [ ] Κλείσιμο του spec open item **fail-after-complete** (επικυρώθηκε 28/08: complete
      μία φορά = complete για πάντα· μεταγενέστερο fail = ορατό extra)
- [ ] `r24` regex ένα-token cure (`raise\s+exception`) — fails-safe όπως είναι
- [ ] Σημειώσεις τεκμηρίωσης: block-comment-strip ordering, mission-field blindness,
      seq-1 tolerance
- [ ] `rls_auto_enable` degrant

## Εκκρεμότητες πηγών

- [ ] Απάντηση «BASE LEG STALL»
- [ ] Stale σημεία του README (7 σημεία εκτός § 9.6)
